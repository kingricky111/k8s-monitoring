grafana:
  defaultDashboardsTimezone: browser
  admin:
    existingSecret: grafana-admin-credentials
    userKey: admin-user
    passwordKey: admin-password
prometheus:
  prometheusSpec:
    retention: 7d

alertmanager:
  enabled: true